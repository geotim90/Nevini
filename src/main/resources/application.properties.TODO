# Bot
bot.owner.id=TODO
bot.prefix.default=>
bot.prefix.mention=false
bot.prefix.name=Nevini
bot.server.invite=TODO

# Datasource
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:file:./nevini
spring.datasource.username=TODO
spring.datasource.password=TODO

# H2
spring.h2.console.enabled=false

# JDA
jda.active=TODO
jda.shards=TODO (1 shard per 1000 guilds is recommended)
jda.token=TODO

# JPA
spring.jpa.hibernate.ddl-auto=validate
