# Datasource
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:file:./nevini
spring.datasource.username=TODO
spring.datasource.password=TODO

# H2
spring.h2.console.enabled=false

# JDA
jda.active=true
jda.shards=TODO (1 shard per 1000 guilds is recommended)
jda.token=TODO

# JPA
spring.jpa.hibernate.ddl-auto=validate

# Nevini
nevini.prefix.default=N>
nevini.prefix.mention=true
nevini.prefix.name=Nevini
